{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "clean": "node scripts/clean.js",
    
    "web:dev": "yarn workspace web dev --host",
    "server:dev": "yarn workspace server dev",
    "mobile:dev": "yarn workspace mobile dev",
    "mobile:ios": "yarn workspace mobile ios",
    "mobile:android": "yarn workspace mobile android",

    "dev": "node scripts/cross-os-terminal.js \"yarn server:dev\" \"yarn mobile:dev\" \"yarn web:dev\"",
    "dev:web": "node scripts/cross-os-terminal.js \"yarn server:dev\" \"yarn web:dev\"",
    "dev:mobile": "node scripts/cross-os-terminal.js \"yarn server:dev\" \"yarn mobile:dev\"",
    "dev:ios": "node scripts/check-ios-simulator.js && node scripts/cross-os-terminal.js \"yarn server:dev\" \"yarn mobile:ios\"",
    "dev:android": "node scripts/check-android-sdk.js && node scripts/check-android-emulator.js && node scripts/cross-os-terminal.js \"yarn server:dev\" \"yarn mobile:android\"",

    "build:web": "yarn workspace web build",
    "build:server": "yarn workspace server build",

    "build:android": "node scripts/check-android-sdk.js && yarn workspace mobile prebuild && cd apps/mobile/android && ./gradlew assembleRelease && cd ../../../ && node scripts/move-android-build.js",
    "build:android:dev": "node scripts/check-eas.js && yarn workspace mobile build:android --profile development",
    "build:android:prod": "node scripts/check-eas.js && yarn workspace mobile build:android --profile production",

    "build:ios": "node scripts/check-eas.js && yarn workspace mobile build:ios --local --output ../../dist/mobile",
    "build:ios:dev": "node scripts/check-eas.js && yarn workspace mobile build:ios --profile development",
    "build:ios:prod": "node scripts/check-eas.js && yarn workspace mobile build:ios --profile production"
  },
  "dependencies": {
    "expo": "~52.0.23",
    "react": "18.3.1",
    "react-native": "0.76.5"
  }
}
